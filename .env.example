# Optional: enables shared comments on a deployed site. Without these values,
# comments are saved to the current browser and can be exported as JSON.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key
